/*
 * Public browser configuration only. This file must never contain a secret,
 * service credential, private customer data, or server-only environment value.
 * Fill these values only after the Supabase project and exact CSP origin are verified.
 */
window.CRYO_ERP_CONFIG = Object.freeze({
  supabaseUrl: null,
  supabasePublishableKey: null,
});

